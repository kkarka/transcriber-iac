apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: frontend
  ports: {{ .Values.frontend.servicePort }}
  targetPort: {{ .Values.frontend.port }}
